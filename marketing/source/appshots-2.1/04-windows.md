# Clear cross-window boundary

- File: `04-windows.png`
- Communicates: Tabs from another Chrome window are available without pretending that cross-window hover preview is possible.
- Visible proof/features: All windows is active, tab and window totals are separate from the Chaos Score, and the `Switch to window 2` item explicitly says preview is unavailable until clicked.
- Suggested headline: See every window, switch only when you choose
- Cautions: Cross-window tabs require an explicit click. Never claim that moving the pointer previews a tab in another window.
